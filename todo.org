#+TITLE: Switchyard — roadmap and deferred work
#+TODO: TODO INPROGRESS WAIT | DONE CANCELLED
#+startup: logdrawer

Forward-looking residue. The reactive-input + scenario framework
+ integration-test + CI tracks all landed; what's left is the
browser-automation push, a few deferred features, the unfixed
review findings, and a handful of open design questions.

Layout: =Roadmap= holds plannable feature work, =Review backlog=
holds verified findings waiting on a design call, =Open design
questions= holds the unsettled d-items, and =Archive= keeps the
DONE entries for the record.

* Roadmap

** B. Browser-driven UI tests

The Rust integration tests in =tests/= cover the HTTP and gRPC
boundaries but stop at the JSON shape — none of them exercise the
SPA. The dashboard panel, the topology canvas's drag / connect
gestures, the REPL's autocomplete + electric-pair + rainbow
overlay, and the chart-update path through the WebSocket are all
unverified by anything other than manual smoke. The plan below
turns that into automated coverage.

Tool choice: *Playwright* (Node, headless Chromium / WebKit /
Firefox, auto-wait built in, native screenshot diff). It's the
default for projects without a JS framework — the alternative
(Cypress) is Chrome-mostly and heavier; Selenium is verbose. One
=playwright.config.ts= + a =tests-browser/= directory.

Determinism is the real pain. The topology graph layout is
seeded but not reproducible across machines (font metrics,
vis-network's animation timing); the physics tick + history
sampler push WS frames continuously; consumer-curve / cloud-curve
animations move power around at 200 ms. Tests need to either
freeze the world at known points or assert on stable structure
rather than pixel-exact screenshots.

*** TODO B1. Playwright harness + first round of functional tests   :testing:

Lay the foundation; cover surfaces that don't need pixel-perfect
output to verify.

- Add =tests-browser/= with =package.json=, =playwright.config.ts=,
  and a single browser project (Chromium) — keep CI fast.
- Fixture =tests-browser/fixtures/server.ts= that boots the
  switchyard binary on a free UI port (and a free gRPC port to
  not collide with anything else), waits for the
  =Switchyard UI listening= readiness marker, returns
  ={ uiUrl }=. Tear-down kills the process.
- Tests: page loads at =/=, =/api/topology= returns a non-empty
  graph, clicking a node populates the inspect pane, the REPL
  textarea accepts =42= and renders =42= in the output.

Acceptance: =npm test= passes on a clean checkout in <30 s; one
flake-free pass over 10 consecutive runs.

*** TODO B2. Scenario flow + REPL interaction tests                 :testing:

Build on B1 to cover the more state-ful flows.

- Drive a small scenario through =/api/eval= (=(scenario-start
  …)=, =(set-meter-power 100 (lambda () N))=), open the Report
  panel, assert =peak_main_meter_w= updates within a few
  polling cycles.
- REPL: type =(set=, assert the autocomplete popup shows
  =set-meter-power=, Tab inserts it. Type =(=, assert the
  electric-pair handler inserts =)= and the cursor lands
  between. Inspect the rainbow overlay's DOM (=.paren-0= /
  =.paren-1=) for a known input string.
- WebSocket-driven chart update: register a battery, drive a
  setpoint via gRPC, wait for the next history sample, assert
  the charts panel renders a non-empty series for that
  component.

Acceptance: each flow runs deterministically when the server is
fresh; tests don't share state between runs.

*** TODO B3. Visual regression on a small set of stable views       :testing:

Pixel-exact snapshots only where the surface is reliably stable.
Avoids the topology-layout flake.

Candidates:
- Help dialog (=#help-dialog= contents) — pure markup, font-sensitive
  but small.
- Scenario report card with deterministic synthetic numbers
  (drive via =/api/eval= setting fixed values, then snapshot).
- REPL textarea with a canned input showing all 7 paren depths
  + a string + a comment, snapshot the overlay.

Skip the topology canvas: vis-network's animation + minor
font-metric drift across runners makes pixel diffs flake.

Use Playwright's =toMatchSnapshot()=; baselines committed under
=tests-browser/__screenshots__/=. Tolerance set to a small pixel-
diff threshold rather than 0 so anti-aliasing variance doesn't
fail every run.

Acceptance: three snapshot tests, baselines checked in, second
run on the same machine matches.

*** TODO B4. CI integration for the browser tests                   :testing:

Add a fourth job to =.github/workflows/ci.yml=:

- Sets up Node, runs =npx playwright install --with-deps chromium=
  (cached on the lockfile hash).
- Builds =cargo build --bin switchyard= (cache shared with the
  rust job).
- Runs =npm --prefix tests-browser test=.

On failure, upload =playwright-report/= as an artifact so
screenshots / traces are inspectable post-run. Run on
=push= + =pull_request= same as the other jobs.

Acceptance: CI green on this PR; introducing a UI regression
flips it red and the artifact contains a trace pointing at the
broken assertion.

** U. On-demand microgrid lifecycle

*** TODO U1. Unload/drop a loaded microgrid from memory      :rust:design:

Counterpart to on-demand loading (the config.lisp-dissolution
track, discussed 2026-07-23): drop a registered microgrid and its
runtime without touching its files on disk — load brings it back.

The enabler is a per-mg runtime-handle bundle plus a symmetric
=microgrid_unregistered= event, mirroring how dynamic ADD already
works end to end (=make-microgrid= → =microgrid_registered= → the
binary spawns gRPC listener / physics / history sampler / loopback,
the WS pump adds a forwarder):

- Binary: keep {gRPC shutdown trigger (=serve_with_shutdown=),
  physics + sampler JoinHandles, loopback abort} per mg id,
  populated in both spawn sites (boot loop and the registered-event
  arm — near-identical code, could unify while at it). On
  unregistered: trigger the gRPC shutdown FIRST — in-flight
  telemetry stream tasks hold component Arcs and would keep
  streaming a frozen site; killing the transport ends them — then
  abort the rest and drop the bundle.
- Lib: =(unload-microgrid ID)= under the interpreter lock — guard
  against a running scenario (v1: refuse; D7's per-mg scenario
  scope is the real fix), remove the registry entry, clear
  =current_microgrid= if it points there, emit unregistered. WS
  forwarders need nothing: last site Arc drops → event Sender
  closes → the forwarder's =RecvError::Closed= arm exits.
- Wrappers: =DELETE /api/mg/{id}=, a swctl verb, a UI button.
- Ports rebind on re-load; the make-microgrid port-collision check
  errors cleanly if something else took the port meanwhile.

Open decisions: drop the mg's dispatches on unload (with Deleted
events so streams + UI update) vs. keep them for the next load;
and whether unload-by-script (a script may register several mgs)
is worth a verb on top of unload-by-id.

** S. Additional gRPC services

*** TODO S1. Expose the reporting API                              :rust:api:

Mount the [[https://github.com/frequenz-floss/frequenz-api-reporting][frequenz-api-reporting]] gRPC service so downstream apps can hit a
switchyard instance with the same reporting calls they'd use against
a real deployment.  Larger project than the assets API (see
=Archive=) — reporting wants historical aggregations, multi-metric
queries, time-range pagination, etc., which switchyard doesn't
currently retain.

*** TODO S2. ListMicrogrids discovery RPC                               :api:

Follow-up from the assets API: there's no =ListMicrogrids= RPC in
the upstream proto, so gRPC-only clients still need
=/api/microgrids= (HTTP) to discover available ids. Worth a
switchyard-side proto extension if a gRPC-only client ever needs
it.

** R. REPL editing experience polish

The shipped first cut covers the common case: =/api/format=
behind =Tab=, paren-stack indent on =Enter=, and a special-form
vs function-call distinction in the overlay. What's left are
correctness gaps where the simple "first symbol after =(= is a
function head" model misclassifies things.

*** TODO R1. =Enter=-on-empty-line outdent                               :ui:

=indentForNewline= always indents at the innermost open paren's
column + 2. Hitting =Enter= twice in a row from a fresh blank
line therefore lands the cursor at the same indent on every
=Enter=, with no easy way to walk back out without typing
=Backspace= a known number of times.

Match Emacs Lisp mode: if the line being broken is whitespace-
only *and* the previous line is also whitespace-only, fall back
to column 0 (or, more general: outdent by =indent-step=). Small
addition to =indentForNewline=, no server roundtrip.

** E. Smaller deferred items

*** TODO E1. Compact: rewrite per-mg :topology body                 :ui:lisp:

UI button → =POST /api/mg/<id>/compact=. Server walks the
microgrid's =MicrogridSite=, emits a fresh =:topology= lambda body,
replaces the body inside the matching =(make-microgrid …)= form in
=microgrids/config.<id>.lisp= in place, deletes
=microgrids/config.<id>.overrides.lisp=. Needs the per-component
=OriginalArgs= snapshot stored at registration time (each
=AsPlist!=-derived args struct re-serialized via =tulisp-fmt=).

Why deferred: real value depends on having heavy override files to
flatten; until users accumulate one, hard to validate the output
shape.

*** TODO E2. Form auto-generation from =AsPlist!=                        :ui:

Right-pane edit form is currently hardcoded per category in JS.
Long-term: a =/api/component-schema= endpoint returns each
=%make-*='s argument list and types from the =AsPlist!= macro
output, and the UI builds the form from that. Removes the
JS-vs-Rust schema drift.

Why deferred: requires either a tulisp-side reflection API or a
=switchyard::lisp::make= self-describing module. Hardcoded JS works
fine for the current category count.

** P. Physics realism upgrades

The electrical layer is deliberately behavioral (no power flow, no
losses) — see the architecture notes in AGENTS.md. The two items
below are the cheapest upgrades that a downstream control app's own
accounting would actually notice; island-mode frequency coupling
(swing equation) was considered and deferred — not needed while
every scenario runs grid-tied.

*** TODO P1. Battery round-trip efficiency + idle draw             :fidelity:

Today charging and discharging both move SoC at 100 % efficiency and
an idle battery holds SoC forever. A control app that schedules
charge/discharge cycles and audits the energy balance sees numbers a
real site never produces. Add to =BatteryConfig=:

- =:efficiency-pct= (round-trip, default ~92): apply √η on each leg —
  charging stores =P·dt·√η=, discharging draws =P·dt/√η= — so a full
  cycle loses the configured fraction and the loss is visible on
  whichever leg the app measures.
- =:idle-draw-w= (default a few W): constant self-discharge +
  BMS/electronics draw integrated every tick regardless of setpoint,
  so SoC sags slowly over a long soak run.

Both default-off-able for configs that want the old ideal behaviour
(=:efficiency-pct 100 :idle-draw-w 0=). SoC integration stays in
=Battery::tick=; the meter aggregation path is untouched (losses are
internal to the battery, the inverter still reports commanded AC).

Bounds caveat (from the 2026-06-04 review): the gateway's AC∩DC bounds
intersection in =aggregate_child_bounds= is only numerically correct at
η=1. Once efficiency lands it must account for the conversion, or the
setpoint gate validates against mismatched AC-side and DC-side envelopes.

*** TODO P2. Battery voltage-vs-SoC curve                          :fidelity:

=dc_voltage_v= is a constant from config, so =dc_current_a=
(= apparent/V) never varies with state of charge — real packs sag
toward the bottom and rise toward the top of the SoC window, and
BMS dashboards key on exactly that. Replace the constant with a
simple piecewise-linear curve parameterised by (=:voltage-min=,
=:voltage-nominal=, =:voltage-max=) anchored at (soc_lower, 50 %,
soc_upper), keeping =:voltage-v= as the nominal for backward
compatibility. Telemetry-only change — power physics stays on W, so
no setpoint / bounds behaviour shifts.

** D. Downstream-app end-to-end testing in CI

Ship switchyard as a standalone binary (and Docker image) so a downstream
app's CI can spin it up as a high-fidelity fake microgrid, drive a scenario,
and gate on the result — exercising the app end-to-end through the real
frequenz gRPC API rather than a hand-rolled mock. The app under test runs as a
separate process talking gRPC; switchyard drives the environment and records
the assertions (=scenario-expect= → =swctl scenario report --assert=).

*** TODO D3. Routable bind address (deferred — only if cross-container)     :rust:ui:

Routable =--ui-bind= / =--grpc-bind <host>= (vs today's loopback) so two
*separate* containers — switchyard + the app — can talk by service name over a
bridge network. Deferred: that's the *only* deployment that needs it, and it
exposes =/api/eval= (arbitrary Lisp = RCE) to the network. Every other E2E shape
runs on loopback, which D1+D2 already cover: host-process CI, a single container
with both binaries, or =docker run --network host=. Pick this up only when a
consumer concretely needs bridge-networked / cross-host separation — and pair it
with a "trusted networks only" startup warning + a lockdown decision (e.g.
=--no-eval= when bound non-loopback). The cross-origin hardening that used to
live here is now D10 (worth doing on loopback regardless).

*** TODO D5. Distribution + crates.io publish                        :packaging:

Binary distribution DONE: the Python client ships as a =maturin= platform
wheel bundling =switchyard= + =swctl= (python/, on python-client), so
=pip install switchyard= needs no separate binary download. The tulisp family
is now published to crates.io (=tulisp= 0.29.0, =tulisp-async= / =tulisp-fmt=
0.1.0) and switchyard depends on them there — the old git-deps blocker is
cleared.

Remaining before a public release:
- Set =GPL-3.0-or-later= + add a =LICENSE= file (Cargo + pyproject); the wheel
  bundles GPL binaries, so this gates a PyPI *and* a crates.io publish.
- A per-OS/arch wheel CI matrix (maturin / cibuildwheel) + a Python test/lint
  job; today only a hand-built linux-x86_64 wheel exists.
- Optional: a Docker image (GPL) consumers run in CI — no source, no linking.
- A crates.io publish of switchyard itself also needs the vendored proto build
  buildable from the published crate (git submodule today), and the
  =frequenz-microgrid-component-graph= dependency moved off its git pin
  (=git = …, rev = "8bab132"= in Cargo.toml) — crates.io rejects git deps.

*** TODO D7. Per-microgrid scenario scope                            :scenarios:

Today a scenario is enterprise-wide: =scenario-start= fans out to every site and
resets all journals, and the report endpoint reads the current site — so two
scenarios can't run concurrently on two microgrids in one process, and
multi-microgrid assertions all land in one site's report. Scope a scenario (and
its journal/report) to a single microgrid so independent cases — or several apps
under test — can run in parallel inside one switchyard. Enables the in-process
multi-microgrid option D6 sets aside; until then, CI-job parallelism covers it.

*** TODO D8. Deterministic clock-sync with the app under test          :testing:

E2E with an external app is necessarily real-time + wall-clock (the app is a
black box on its own clock), so checks must assert settled-state invariants with
tolerance and a run costs minutes. A shared virtual-time contract —
switchyard's stepped clock advancing in lockstep with the app — would make E2E
fast and bit-reproducible, but needs the app to honour an external tick. Worth it
only if a consumer opts in; the absolute-schedule live-cue gap (J-era) belongs
here too.

*** TODO D9. Embedded-prelude follow-ups (optional, non-blocking)      :rust:dsl:

The minimal embed (D1) is in and is the accepted long-term shape:
=Config::new_inner= bakes the three =sim/*.lisp= files into the binary via
=include_str!= + =eval_string= and loads them before the user config, so a config
needs no =(load "sim/…")=. Two independent follow-ups, neither urgent:

- *Accurate error spans — gated on tulisp, not expected soon.* =eval_string= has
  no filename, so a prelude parse/eval error cites =<eval_string>= rather than
  the real file. If tulisp ever grows embedding-files support (an
  eval-with-filename path), swap the embed to evaluate each file under its real
  path (a =concat!=-based macro over literal path tokens — no runtime loop). Low
  value: the prelude is stable in-tree code, so such errors are rare and
  dev-time only — fine to live with =<eval_string>= indefinitely.

- *Relocate to =src/lisp/prelude/= — independent of tulisp; do it anytime.* Moves
  the three files out of the user-facing samples into embedded crate source. But
  it forces the =config.lisp= cleanup — the =(load "sim/…")= / =watch-file= lines
  break once the files move — which retires prelude hot-reload. So it's a
  deliberate dev-workflow call, not a freebie; left as-is until that tradeoff is
  worth making.

** Y. Python integration-testing library (PyPI)                :python:testing:api:

IMPLEMENTED — on the =python-client= branch (see =python/README.md= and
=python/src/switchyard/=). A synchronous, pytest-first client that drives
switchyard the way a downstream control app would: build a topology (nested
=successors= → =(make-*)= Lisp), launch + manage the process, drive the
environment, inject faults, read component telemetry over gRPC and graph
aggregates over the HTTP formulas, and assert settle-aware — plus author
scenarios in Python and a deterministic serverless gate
(=run_scenario_stepped=).

Phases Y0–Y8 done: process lifecycle, gRPC + HTTP transport, config builder,
runtime mutation (=set= / =drive=), settle-aware assertions, scenarios + a
pytest plugin, and packaging. Shipped as a =maturin= platform wheel bundling
the =switchyard= + =swctl= binaries (=pip install switchyard= just works);
uv-native dev; the tulisp family is on crates.io (0.29.0 / 0.1.0).

Deferred follow-ups: an async surface (sync-only today); a per-OS/arch wheel
CI matrix + PyPI publish (needs =GPL-3.0-or-later= set first — see D5); a
library↔binary version-compat check; typed wrappers for the dispatch / assets
APIs and a =timeline(hold/ramp)= drive helper; and deterministic
app-in-the-loop (a clock-step RPC, ties to D8).

* Review backlog — 2026-06-01

Whole-codebase review (8-agent subsystem fan-out + clippy/fmt; clippy
came back clean). The straightforward issues were fixed in the same pass
(git =b9673b2..f615211=); the items below were left out because each
needs a design call or a non-local change. All High/Medium verified
against the code.

** High

** Medium

*** TODO [#B] Snapshot save/load ignore the per-request microgrid scope :concurrency:

=snapshots_save= / =snapshots_load= (=src/ui/handlers/snapshots.rs=)
resolve the target via the ambient =current_microgrid= (set by the last
per-mg eval) rather than a caller-specified id, so they can snapshot /
restore the wrong microgrid. Same root cause as the concurrent-eval
finding above.

Fix: add explicit microgrid-scoped snapshot routes and apply the same
atomic-scope fix.

* Review backlog — 2026-06-04

Second whole-codebase pass (5-agent subsystem fan-out; consistency /
integration / simulation-fidelity lens), verified against source. Fixed
this pass as small commits: validate request lifetime before actuating
(=88eb325=); always allow 0 W (=a93b69d=); reject malformed
augmentations (=ddb529a=, closes the inverted-bounds Low item above);
config-time =:command-mode= can no longer re-enable an errored device
(=d6478cd=); an errored / standby inverter trips offline to zero output
(=27e079d=). The items below were left deferred — each needs a design
call or a non-local change.

** Fidelity

*** TODO [#B] Reactive config bounds sampled at live P, not rated — research first :reactive:fidelity:
=make_component_proto= (=src/proto_conv.rs:107=) fills the static
=AcPowerReactive= =MetricConfigBounds= from =c.reactive_bounds()=, whose
inverter impls sample at *live* P (=battery_inverter= =measured_w=,
=solar= =ramp.actual()=) — but the comment claims "full rated active P".
So an idle PF-limited inverter advertises =(0, 0)= reactive capability in
=ListElectricalComponents=, and a controller that caches the listing
concludes it can't do Q.

Research on the internet before fixing (not urgent): real inverters
differ — some are fully reactive-capable up to the kVA circle at any
active power (including P≈0), others can only source/sink Q as a
percentage of the *current* active power. Decide which model(s)
switchyard should expose and how the static config bounds (rated) vs the
live telemetry bounds should each read. Likely answer: config bounds use
rated P (=p_max=), telemetry keeps live-P. Check IEEE 1547-2018
reactive-at-zero-P capability and typical nameplate Q ratings.

*** TODO [#B] Reported PV bounds ignore sunlight curtailment :fidelity:bounds:
=SolarInverter::effective_active_bounds()= returns rated ∩ augmentation,
so telemetry advertises e.g. =[-30 kW, 0]= at 50% sun while the inverter
only delivers -15 kW. I1 made the *output* curtail to =min_avail_w=, but
the *bounds* still don't reflect irradiance, so a controller bidding max
generation sees headroom that isn't there. Fold =min_avail_w= into the
reported lower bound — but confirm against the real frequenz API (does it
report irradiance-limited inclusion bounds for PV?).

*** TODO [#B] dc_power_w is signed-apparent but its bounds are active-only :telemetry:reactive:
A battery's =DcPower= telemetry value is =√(P²+Q²)·sign(P)= while the
attached bounds are the active SoC-derated envelope (=battery.rs= /
=proto_conv.rs=). Value and bounds sit on different scales whenever Q≠0;
the scenario energy integrator also over-states charge/discharge Wh by up
to the PF headroom. Report active P as the value, or attach apparent
bounds.

*** TODO [#B] CHP and Grid stream no power telemetry of their own :telemetry:
=Chp= is a stub (parent meter carries =:power=) and =Grid::telemetry=
emits only id+category (the downstream meter carries flow). Faithful to
the "meter carries the literal" design, but a controller written to the
real API (subscribe by category = Grid / Chp) gets an empty metric set.
Document the convention at the API boundary, or synthesize an
=AcPowerActive= sample on these components.

*** TODO [#B] Telemetry state-code is decoupled from health       :telemetry:
=component_state= is derived from power sign (=power_state(p)= →
idle/charging/discharging); the ERROR =ElectricalComponentStateCode= only
comes from =TelemetryMode::ErrorEmpty= (=proto_conv.rs:292=). So an
=:health 'error= inverter (now zero-output after =27e079d=) still reports
state *idle*, indistinguishable from a healthy parked one, unless its
telemetry mode is also set. Decide whether health should drive an ERROR
state snapshot or stay orthogonal, and document it. (This is the open
part of I3.)

** Consistency

*** TODO [#B] Two TTL clocks: Instant (setpoints) vs Utc (augmentations) :api:bounds:
=TimeoutTracker= keys setpoint expiry on monotonic =Instant=;
=ComponentBounds::drop_expired= uses wall-clock =Utc=. An NTP / VM clock
step expires augmented bounds without touching setpoint timeouts, so the
two TTL guarantees handed to clients clock against different references.

** Structural / concurrency

*** TODO [#B] sum_single silently drops multi-band bounds            :bounds:
=VecBounds::sum_single= (=src/sim/bounds.rs=) keeps only =.first()= of
each child's bounds, so =aggregate_child_bounds= (the gateway setpoint
gate) ignores later bands of a multi-band inverter. Single-band today,
but unenforced — a future multi-band rated inverter regresses the gate
silently.

*** TODO [#B] aggregate_child_bounds skips the parallel-share divisor :bounds:fidelity:
=aggregate_child_bounds= (=src/sim/microgrid_site.rs=) sums children's
effective bounds without the =/ parent_count= share that =meter.rs:68=
applies to power. A child shared between two parents contributes its full
bounds to each gate but only half its power to each meter — the gateway
then allows setpoints 2× the parallel-share semantic. (Couples to d3.)

* Review backlog — 2026-06-05

Second exhaustive pass (7-agent subsystem fan-out + an adversarial review
of this session's commits =0439e76..ddb529a=, and first coverage of the
UI backend, dispatch/assets services, bins, and lisp runtime). The seven
commits held up under the adversarial pass — two flagged "regressions"
were false positives (PV resuming an armed *curtailment* setpoint is
intended, and =effective()= is always intersected with finite rated
bounds so =clamp= is never a half-open identity). The items below are
deferred; nothing fixed this pass.

** Fidelity / consistency (new)

*** TODO [#B] Meter reactive aggregation drops :power-meter and CHP Q :reactive:telemetry:
=Meter::aggregate_reactive= (=src/sim/meter.rs=) returns 0 when
=power_source= is set, and =Chp= carries no reactive state, so a CHP /
load modelled via a =:power= meter contributes no Q to its parent. The
main meter's =reactive_power_var= (and any PF / apparent-power derived
from it) under-reports.

*** TODO [#C] Meter + assets status are always healthy            :telemetry:
=Meter::telemetry= always reports state =ready= regardless of children
health/flow; =assets_server= =get_microgrid= always returns
=MicrogridStatus::Active= even when every component is errored. A client
gating on degraded status never sees one. Cosmetic for a simulator but a
fidelity gap.

** Robustness / UI hardening (latent until --ui-bind ships)

*** TODO [#B] Add CORS + confirm body limits before --ui-bind  :hardening:ui:
The UI is loopback-only today. Before =--ui-bind= lands: add a
=tower-http= =CorsLayer= (a cross-origin page could otherwise POST
=text/plain= Lisp to =/api/eval= and run arbitrary code incl. file
writes), and apply =DefaultBodyLimit= to =/api/eval=, =/api/format=,
overrides-text, and the =BulkRemoveBody.indices= vec (unbounded). The
body-limit gap and the snapshot-save/load ambient-scope hazard are already
recorded (2026-06-01) — fold them into the same hardening pass.

*** TODO [#C] Re-emit latest snapshot over WS on a lagged Sample bus :ui:ws:robustness:
The dashboard guards against dropped =Sample= WS frames client-side
(=dashboard.js startAutoReseed=: a 5s =/microgrid/latest= poll +
=visibilitychange= reseed). The deeper fix is server-side: when a
per-mg Sample forwarder hits =RecvError::Lagged= (=src/ui/events_ws.rs=,
the =spawn_forwarder= loop just logs + =continue=s), re-emit the
loopback slot's =latest= cache so every connected client converges from
one server action — the registered/registry forwarders already do this
on their own =Lagged=. Deferred because the forwarder only holds the
=MicrogridSite= bus receiver, not the =loopbacks= map (=slot.latest=
lives in =src/ui/loopback.rs=), so it needs the loopbacks threaded into
=event_pump= + synthesised =SiteEvent::Sample= frames — a cross-subsystem
change. The client-side poll covers it until then.

** Low / minor (new)

- =runtime_modes.rs= =From<Health/…> for TulispObject= emits a *string*,
  not a symbol, so an enum round-tripped through a plist would type-mismatch
  (=:health 'error= needs a symbol). Latent — no defun returns enum plists;
  blocked on a tulisp API for interning without a ctx (=From= can't reach
  one).

* Review backlog — 2026-06-06

Follow-up review after the dispatch series + module splits merged.
The small items (stale comments, doc drift, CI submodule checkout,
unescaped dashboard names, csv-lookup NaN guard, snapshot-load
atomicity, dispatch phantom-update) were fixed in the same pass; the
items below need more than a local change.

** TODO [#C] swctl --json emits Rust Debug output for gRPC-backed commands :api:

=--json= is documented as "emit JSON … where applicable", and the
HTTP-routed verbs (=scenario=, =scenarios=, =snapshot=, =pool=) do
emit real JSON via serde. But every gRPC-backed command (=info=,
=list=, =connections=, =stream=, the whole =dispatch= family) prints
=println!("{resp:#?}")= — prost types don't implement =Serialize=,
and Debug format is not parseable JSON, so scripting against these
verbs silently gets a different language. Options: hand-rolled
=serde_json::json!= views per response (the dispatch family already
has =DispatchView=-shaped conversions in =src/ui/handlers/dispatches.rs=
to crib from), a =prost-wkt= style reflection dependency, or
documenting the limitation honestly in =--json='s help text. Either
fix or rename until fixed.

2026-07-16 review addendum: =set-power= and =augment-bounds= do not
even receive the =json= flag — they ignore =--json= entirely rather
than printing Debug. Fold them into this fix.

** TODO [#C] index.html + style.css fail biome's recommended rules       :ui:

CI lints only the hand-written JS (all clean). Running biome over
=ui-assets/= also flags 17 errors + ~29 warnings in =index.html= and
=style.css=: missing =type= on =<button>=s, =<svg>=s without titles
(a11y), =noDescendingSpecificity= and a clutch of =!important=s in
the stylesheet. Worth a dedicated cleanup pass that then widens the
CI lint to the whole directory; mechanical but touches a lot of
lines, and the specificity fixes need visual verification.

* Review backlog — 2026-07-16

Whole-codebase review (7-agent subsystem fan-out + clippy + full
test suite + live smoke run; clippy and tests came back clean).
The straightforward findings were fixed in the same pass (git
=ed37ac2..bb85df7=); the items below each need a design call or a
non-local change.

** Correctness

*** TODO [#B] Setpoint expiry-vs-renewal race can wipe a fresh command :concurrency:api:

The 100 ms timeout loop first drains expired =(id, axis)= keys
(=TimeoutTracker::remove_expired=), then calls =reset_setpoint_axis=
outside any lock. A =SetElectricalComponentPower= landing between
the drain and the reset (or between actuation in =server.rs= and
=add_timeout= — two non-atomic steps) is applied, acknowledged with
a fresh =valid_until=, and then clobbered by the reset armed for
the stale deadline. The component parks at default while the
client believes a 15-minute command is running; nothing corrects
it. Fix: arm the deadline atomically with (or before) actuation,
or give each axis a setpoint generation the reset compares.

*** TODO [#B] Legacy GET endpoints race the ambient microgrid scope :concurrency:ui:

=/api/topology=, =/api/history= and =/api/setpoints= resolve via
=config.site()=, which reads =current_microgrid= without the
interpreter lock its contract requires. While a long
=POST /api/mg/5/eval= holds the lock with the scope flipped, a
concurrent legacy GET returns microgrid 5's data. =control.rs=
already dodges this by taking the first registry entry. Design
call first: should "legacy" mean /first microgrid/ or /currently
scoped/? (=topology.rs='s module doc says "the bootstrap site
(legacy)", which matches neither.) Same family as the 2026-06-01
snapshot-scope item.

*** TODO [#B] Concurrent disjoint augmentations can empty the envelope :bounds:concurrency:

=validate_augmentation= checks the proposed bands against
=effective_active_bounds()= before its own =augment_active_bounds=
applies, so two clients augmenting concurrently with mutually
disjoint bands each see a compatible pre-state, both apply, and
the effective envelope goes empty — every setpoint rejected while
the running output stays unconstrained. Self-heals when a TTL
lapses. Fix needs validate+apply under one bounds lock, i.e. a
component-side =try_augment= API.

*** TODO [#B] Inverter share divisor counts non-DC children      :fidelity:

=BatteryInverter::tick= distributes the commanded value over /all/
healthy graph children, but =set_dc_active_reactive= is a default
no-op — a mis-wired child (say a meter connected under an inverter
via the UI) is counted in the divisor, silently swallows its
share, and the inverter still publishes the full commanded P with
no telemetry revealing the gap. Options: filter to children that
accept DC pushes (needs a trait predicate), or reject the connect
at topology time.

*** TODO [#B] Overrides removal can break the reload         :ui:lisp:reload:

Deleting a =(make-* :id N)= form from the Overrides dialog while a
later persisted =(connect P N)= (or =(set-component-operational-mode
N ...)=) form survives leaves an orphaned reference. On the next
reload =load-overrides= just =(load)=s the file, so the now-strict
=connect= validation (unknown id -> error, added 2026-07-16) errors
out of the =make-microgrid= =:topology= lambda and fails the whole
=reload_locked= — which has ALREADY reset every site. Result: 500
from the delete handler and every microgrid left empty until the
file is hand-edited. Same trigger from a hand-edited or
double-appended overrides file (this bit the shipped
config.2203.overrides.lisp, which had a doubled site import).

Verified a WIDENING of a pre-existing hazard, not new:
=set-component-operational-mode= already errored on an unknown id
and already persisted (it bumps the structural fingerprint), so a
persisted operational-mode form referencing a deleted make already
hit the same reset-then-fail path. The strict connect check just
extends it to the far more common connect forms. Root cause is
=reload_locked= having no rollback after the up-front reset
(=boot.rs=; a true build-aside-and-swap is off the table because
the physics tasks / gRPC servers / loopback all hold the site
handles in place — see AGENTS.md).

Plan is A+B (B is the guarantee, A is best-effort prevention):

- *B — tolerant per-form replay (the fix).* Replace =load-overrides='s
  =(load path)= with a Rust primitive that =parse_file='s the
  overrides file and evals each top-level form on its own, catching
  the error per form: a failing form is skipped with a =log::warn=
  (surface it in the UI log / graph-status pane too) instead of
  aborting the reload. A whole-file parse error degrades to skipping
  the file, not aborting. This fixes EVERY path into the break
  (dialog orphan, hand edit, doubled import) and is honest about
  what the file is — an append-only journal of single UI edits, each
  appended only after it succeeded once. tulisp notes: =(load)=
  already routes through =eval_file= (not the re-entrancy-buggy
  =eval_string=), =ctx.parse_file= returns the forms unevaluated,
  =ctx.eval= per form is fine re-entrantly (=condition-case= does
  exactly this), and =condition-case= exists lisp-side if a
  lisp-only version is preferred. Mirror =load-microgrid-configs=
  in =src/lisp/defuns/fs.rs= (already a Rust parse+eval primitive,
  and =load_dir= is captured there for relative-path resolution).
  Tests: orphaned-connect file and doubled-import file both reload
  with warnings and a non-empty site.

- *A — dialog-side dependency refusal (prevention/UX).* In
  =remove_persisted_overrides= (=src/lisp/overrides.rs=), before
  rewriting, refuse (409, not the 500 that IO errors get — map a
  distinct =io::ErrorKind::InvalidInput= in both delete handlers)
  when a surviving form would be left referencing a component id
  whose only =make-= form is being dropped. Extract ids from the
  =tulisp_fmt= Cst (=List{children}=/=Atom{text}=, already used by
  =persisted_overrides=): defined id = the =:id N= of a top-level
  =make-=/=%make-= form; referenced ids = integer atoms of a
  top-level reference verb (=connect=, =disconnect=,
  =remove-component=, =rename-component=, =set-component-*=).
  MUST degrade gracefully: =let*= paste forms bind their own
  handles and are self-contained (cross-form refs only use integer
  ids), and any unrecognised head is treated as referencing nothing
  — so A never produces a FALSE refusal that blocks a legitimate
  delete, and B is the net for whatever A misses. A is best-effort
  by design; do not add a per-form-type table that must track the
  whole DSL forever.

Left in chat 2026-07-17: user wants to explore the tulisp side
(per-form eval / condition-case ergonomics) before we build B.

** Design calls

*** TODO [#C] TTL-expiry reset publishes one 0-sample then slews :telemetry:reactive:

Both full-reset paths zero the published value immediately while
the ramp then slews down from the old value, so a telemetry sample
between a TTL reset and the next tick shows a one-sample glitch
(3000 → 0 → 2900 → … → 0), on both axes. Either let the next
tick's ramp value stand (don't zero published on a slewing reset)
or accept the artifact. Interacts with the trip/reset split fixed
in =0fb01e3= (health trips now snap; TTL resets still slew).

*** TODO [#C] Imported sites tick parents before children     :consistency:

=site_import::forms()= emits =(make-*)= forms in export order, so
a battery can register after its inverter — the opposite tick
order from the authored-config convention (children first, because
Lisp evaluates =:successors= before the surrounding =make-*=). The
inverter→battery push then lands same-tick for some imports and
one-tick-lagged for authored configs. Settles either way; a
topological sort of the emitted makes would normalize the step
response.

*** TODO [#C] Site-level runtime-knob setters accept unregistered ids :consistency:

=set_health= / =set_telemetry_mode= / =set_command_mode= use
=entry(id).or_default()= with no registration check (a unit test
relies on it), while =set_operational_mode= rejects unknown ids
with a documented rationale. A ghost entry sticks around until
=reset()=, and a later component allocated on that id inherits the
fault knob. The Lisp defuns validate since =fa39fc3=, so only
direct Rust callers can still hit it — decide whether the site API
should validate too (behavior change + test update).

*** TODO [#C] Dispatch Update cannot clear a recurrence rule          :api:

The update path only applies =recurrence= on =Some(rec)= — a
masked path with a =None= value is ignored, unlike =duration=,
where a masked =None= clears the field. So a recurring dispatch
can never be made one-off; delete+recreate changes the id. Check
the intended proto semantics; mirroring the duration treatment is
a small change once decided.

*** TODO [#C] Scenarios poller never pauses                            :ui:

=panels.js='s =schedulePoll= never cancels, so once the Scenarios
view has been opened, =/api/scenarios= + =/api/scenario= (+
report/events/csv while a journal exists) poll every 3 s forever,
on every view — the module comment says "while the mode is
active". The header's =#active-scenarios= chip does need some
background signal, so the fix is a cadence design call: a slower
background poll when the mode is inactive, or a chip-only summary
poll.

*** TODO [#C] hot_reload test races watcher installation          :testing:

=tests/hot_reload.rs= sleeps 200 ms before rewriting the config,
but =Config::watch()= exposes no readiness signal — on a slow
machine the rewrite can fire before the inotify watch exists and
the test burns its whole 10 s poll budget. Clean fix: a readiness
hook on =watch()= (a oneshot/=Notify= fired once =watcher.watch()=
succeeds); the downstream version-bump poll is already correct.

*** TODO [#C] One-sided rated bounds are dropped          :fidelity:bounds:

Site exports can configure only one side of a component's rated
range, and =site_import= emits =:rated-lower= / =:rated-upper=
independently — but =rated_active_bounds= is a full =(f32, f32)=
pair, so =%make-grid-connection-point= drops a half pair with a
warning and the grid stays unbounded (a hard error here breaks
booting real imported sites). Supporting one-sided bounds means
widening the pair to optional edges through the component trait,
the envelope math and the proto conversion.

*** TODO [#C] REPL (reset-state) only resets the active site  :design:lisp:

=reset-state= → =(reset-microgrid)= resets the router-resolved
site only. On the reload path the other sites are reset Rust-side
in =reload_locked=, so reloads are correct — but a REPL
=(reset-state)= in a multi-microgrid setup leaves the other sites
untouched. Fan =reset-microgrid= out registry-wide, or keep and
document the current scope. (The stale config.lisp header comment
was fixed in =fa39fc3=.)

** Packaging / housekeeping

*** TODO [#B] PyPI name =switchyard= is taken            :packaging:python:

PyPI already has =switchyard= (a GPL network-teaching framework,
v1.0.1), so =pip install switchyard= installs the foreign package
and the planned publish (Y track / D5) cannot happen under this
name. Rename or namespace (e.g. =switchyard-sim=) before any
release. The wheels.yml smoke test installs by wheel path since
=e0d48bd=, so CI no longer trips over it.

*** TODO [#C] sdist is shipped but never verified installable   :packaging:

=wheels.yml= builds an sdist with =manifest-path = "../Cargo.toml"=
while =build.rs= needs the vendored proto submodules — the sdist
likely cannot rebuild outside the repo, and nothing smoke-tests
it. Either include the proto trees in the sdist and smoke-test the
build, or stop shipping the artifact. Overlaps the D5
crates-publish notes.

*** TODO [#C] tulisp =etags= feature is always on                   :build:

Only the feature-gated =switchyard-etags= bin needs it; default
builds (and the wheel) pay for it. Shape: =tulisp = { features =
["sync"] }= plus =etags = ["tulisp/etags"]= on the crate's own
=etags= feature — verify the lib truly compiles without it first.

*** TODO [#C] Orphaned data files                                 :cleanup:

=sim/example_load.csv= is referenced by nothing (its shape is
valid for =csv_profile=) — delete it or reference it from the
csv-load docs.

*** TODO [#C] integrate_energy re-aggregates every meter subtree per tick :perf:

=Meter::telemetry= recomputes the recursive child aggregation on
every call, and =integrate_energy= calls it for every component
each physics tick (default 100 ms) just to read =active_power_w= —
O(Σ subtree sizes) ×10/s on top of the same walk the tick
consumers already do. Fine at current site sizes; cache the value
on tick or add a cheaper power-only read if sites grow.

* Review backlog — 2026-07-17
** TODO [#B] Atomic multi-knob apply for the status endpoint         :api:ui:
:LOGBOOK:
- State "TODO"       from              [2026-07-17 Fr 12:46]
:END:

apply_status pre-validates the runtime knobs against the operational
mode, then applies health, command and telemetry with no common lock
(control.rs notes this inline). A concurrent
(set-component-operational-mode ...) eval can fail a setter AFTER
set_health applied — a half-applied status the pre-check exists to
prevent. Fix wants an atomic validate-then-apply on MicrogridSite
taking all three knobs; that would also collapse the duplicated mode
predicates (the handler pre-checks restate set_command_mode /
set_telemetry_mode's own checks with different error strings).

The 2026-07-17 high-effort review found a second, site-layer TOCTOU
of the same flavor: =MicrogridSite::set_health= / =set_command_mode= /
=set_telemetry_mode= read =operational_mode(id)= *before* taking the
=runtime= write lock, so a concurrent =set_operational_mode= can
complete between the mode read and the knob write — e.g.
=set_health(Ok)= re-opens the command channel on a component whose
config just went =inactive=. The tri-knob atomic apply alone won't
close it: =operational_modes= is a separate lock from =runtime=, so
the fix must also re-read (or hold) the mode under the runtime lock.

** TODO [#C] Inspector setpoints pane queries the ambient site           :ui:
:LOGBOOK:
- State "TODO"       from              [2026-07-17 Fr 12:46]
:END:

renderSetpoints fetches the legacy /api/setpoints, which resolves via
config.site() (ambient scope) — for a component in any non-ambient
microgrid the "Recent setpoints" pane deterministically shows "none",
despite live control traffic. Pre-existing (predates the
formula-explorer branch; mgPath's comment even lists it as
unmigrated). Same root cause family as the "Legacy GET endpoints race
the ambient microgrid scope" entry above, but that one frames a
concurrency race — this symptom is deterministic. Fix: a
/api/mg/{id}/setpoints route + mgPath("setpoints") in inspect.js.

** TODO [#C] One Category -> label/slug table instead of four       :cleanup:
:LOGBOOK:
- State "TODO"       from              [2026-07-17 Fr 12:46]
:END:

Byte-identical ten-arm Category->kebab matches live in
ui/handlers/topology.rs (category_label) and sim/scenario_csv.rs
(category_slug); sim/marker.rs carries a name-prefix copy and
sim/site_import.rs a SCREAMING->make-form copy. Adding a category (or
renaming a slug) must be replayed by hand in each. Known drift
already: swctl short_category prints "?" for the marker categories,
and dispatch category_from_alias cannot target them. One canonical
mapping on Category (Display or a label() impl) would collapse the
Rust copies and give the compiler a say.

** TODO [#C] SPA cleanup batch from the 2026-07-17 review        :ui:cleanup:
:LOGBOOK:
- State "TODO"       from              [2026-07-17 Fr 12:46]
:END:

Verified-lite findings, none user-visible on their own:
- explain.js astToHtml is a twin of formulas.js formulaToHtml (same
  span vocabulary + layout rule), and formulas.js keeps a private
  recursive-descent parser to rebuild the AST the formula endpoint
  already returns as JSON. One renderer over the AST endpoint.
- astToText re-renders each subtree per node (O(n * depth) on deep
  formulas) and renderNumber clones Rust's number Display with known
  drift cases; the endpoint could ship a pre-rendered text per AST
  node (explanation nodes already carry =rendered=).
- splitter.js setupFormulaDrawerSplitter writes localStorage and
  forces a reflow on every mousemove; persist once on mouseup, clamp
  against a height captured at mousedown.
- inspect.js showComponent awaits each per-metric /history fetch
  sequentially; Promise.all them.
- topology.js drawSnapGrid does getElementById + clientWidth/Height
  reads per animation frame during a drag; snapshot at dragStart.
- explain.js refreshFormula renders the Why tree, then
  refreshTreeFolding re-walks every row from the DOM; emit hidden +
  chevron state directly in whyTreeHtml.
- index.html duplicates the canvas-controls strip for both canvases;
  render it from one table (ALIGN_MODES pattern).
- explain.js has six copy-pasted checkbox->query-param blocks; a
  data-param attribute + one loop, and FormulaQuery's serde(default)
  silently accepts misspelled params.

** TODO [#C] Rust cleanup batch from the 2026-07-17 review          :cleanup:
:LOGBOOK:
- State "TODO"       from              [2026-07-17 Fr 12:46]
:END:

- ImportMicrogridResp re-declares CreateMicrogridResp's four fields
  and copies them over one by one; serde(flatten) keeps the shapes in
  lockstep.
- formula.rs parse_ids re-implements the comma-separated id grammar
  of dispatch::parse_target's numeric branch, with different
  dedup/order behavior; share one helper.
- make.rs threads the same four mode kwargs through all seven
  AsPlist arg structs positionally; a shared ModeKwargs fragment
  would centralize the field list.
- site_import.rs lift() hand-rolls category/inverter token decoding
  that proto.rs impl_enum_from_str + proto_conv already centralize;
  parse to the proto enum and match exhaustively.
- microgrids.rs import collision check takes a read lock per
  imported id per site (O(ids x sites) acquisitions); snapshot each
  site's id set once.
* Review backlog — 2026-07-22

Whole-codebase review (8-agent subsystem fan-out + clippy + fmt +
full Rust/Python test suites + biome + ruff/ty + live smoke run; all
checks green before and after). The mechanical findings were fixed
in the same pass; the items below each need a design call.

** Fidelity / API semantics

*** TODO [#B] EvCharger has no own-health gate                :fidelity:
An =:health 'error= / ='standby= charger keeps charging at its armed
setpoint (=ev_charger.rs tick= has no trip; both inverters trip to
zero). Contradicts =integrate_energy=, which freezes the errored
component's energy cursor while meter aggregates keep accruing the
same watts. Decision: give it the inverter-style trip — and pick the
recovery semantic (await re-dispatch like the battery inverter, or
auto-resume).

*** TODO [#B] PV health-trip never trips the reactive ramp :reactive:fidelity:
=solar_inverter.rs= zeroes published Q while tripped but leaves
=reactive.ramp.actual= and the armed command; recovery teleports Q
back (~5 kVAR in one tick), skipping the slew =ReactivePath::trip='s
own doc says the snap exists to prevent. Decision: full
=reactive.trip()= (battery-inverter parity, drops the Volt/VAR
command) vs keep the armed command but snap the ramp actual to 0
(needs a small ReactivePath API addition).

*** TODO [#B] Augment on a non-augmentable component is ACKed :api:bounds:
=augment_active_bounds= is a default no-op (only inverters + EV
charger override), and the gRPC handler ACKs with a =valid_until= —
a bounds-driving app augmenting a battery/meter/grid believes a cap
is armed that doesn't exist. The setpoint path is honest
(=Unsupported= → =UNIMPLEMENTED=). Found independently by two
reviewers. Decision: add a supported/unsupported signal to the trait
and reject at the gateway (which status — UNIMPLEMENTED to mirror
setpoints, or INVALID_ARGUMENT?), or implement augmentation storage
for more categories.

*** TODO [#C] EvCharger TTL-expiry snaps to 0, inverters slew :fidelity:
=EvCharger::reset_setpoint= uses =snap_to= (22 kW → 0 in one tick)
while both inverters treat the same reset as "a control event, not a
physical discontinuity" and slew down. Decision: align on slew-down
or document snap-to-park as intended for chargers.

*** TODO [#C] SetPower response stream skips the ACCEPTED phase   :api:
The proto documents initial-response (ACCEPTED) then final
(SUCCESS/FAILED/OVERRIDDEN); switchyard sends one SUCCESS and closes
(=server.rs do_set_power=). Known frequenz clients tolerate it.
Decision: emit ACCEPTED-then-SUCCESS for fidelity, or document the
single-message simplification.

*** TODO [#C] Augment lifetime fallback is 60 s, proto says 5 s   :api:
An absent =request_lifetime= on Augment falls back to the shared
=default_request_lifetime= (60 s); the vendored proto documents 5 s
for Augment (60 s matches only SetPower). Recovery-timing tests
against switchyard run differently than against production.
Decision: per-RPC fallbacks or one knob + documented deviation.

** Config semantics

*** TODO [#C] set-physics-tick-ms in config.lisp is a no-op   :lisp:design:
It runs while the registry is empty (before =load-microgrid-configs=),
so it configures the never-ticking bootstrap site; each real
microgrid gets the built-in default. After a hot reload it applies
to only the first registry entry — boot vs reload asymmetry.
Decision: make unscoped =set-physics-tick-ms= (and arguably
=set-voltage-per-phase=) fan out registry-wide, or move the call
inside each per-mg =:topology= and fix the section comment.

*** TODO [#C] Scenario-collapsed sunlight never returns to ambient :scenarios:design:
The cloud curve rides the demo inverter's =:sunlight%= lambda source
now, so a scenario's numeric =set-solar-sunlight= collapses it — and
with the old =every= timer gone, nothing reinstalls the ambient
curve when the scenario ends: the demo PV stays at the scenario's
last constant until a config reload. Decide whether scenario end
should restore displaced dynamic sources (snapshot + reinstall in
the runner) or whether last-state-wins is the intended sim semantic
(then say so in the scenario docs).

*** TODO [#C] scenario-record-csv resolves DIR against CWD    :lisp:design:
Unlike =csv-load= / =file-exists-p= / =watch-file= / =(load …)=,
which all resolve against the config's load dir (the divergence
d1/csv_profile already litigated). A server launched outside the
repo dir scatters =csvs/= wherever the process runs. Decision:
outputs-relative-to-CWD is defensible (README says "into ./csvs/"),
but if the convention is load-dir the fix is the same
=load_dir.join= csv_profile uses.

** Python client

*** TODO [#B] A dead metric pump serves frozen values forever :python:api:
=_grpc.py= (both flavors): a stream the server ends cleanly (e.g.
=TelemetryMode.CLOSED=) exhausts the pump; =GrpcStreamBroadcaster=
only retries gRPC errors, so after the fault knob is restored every
read — and every settle-aware =expect= — polls the stale cached
sample until timeout, or passes against pre-fault data. Decision: on
pump exit, evict the cached value (reads return None) and/or re-open
the stream on the next read.

*** TODO [#C] assert_passed() passes when zero checks ran :python:scenarios:
A scenario stopped before its first =(check …)= fires reports
=checks_failed: 0= and the gate goes green having verified nothing.
Decision: fail (or warn) when =checks_passed + checks_failed == 0=,
or compare against the authored =Scenario._checks= count when the
client authored the scenario.

*** TODO [#C] pytest marker gate runs after an already-failed test :python:testing:
The =switchyard_scenario= marker path runs-and-gates the scenario in
fixture teardown even when the test body failed (spending the full
scenario length on a red test), and a failed gate surfaces as a
teardown ERROR, not the FAILURE the docstring promises. Decision:
observe the test outcome via a =pytest_runtest_makereport= hook and
skip the gate on failure, or document the teardown-error semantics.

*** TODO [#C] Sync Site close-race comments overstate the locks :python:cleanup:
A =to_thread= read blocked on =_grpc_lock=/=_pump_lock= during
=close()= proceeds after the drain and inserts a fresh client/pump
that is never closed — the "must not slip a fresh client past the
drain" claim isn't actually enforced (impact bounded: daemon
threads, the racing assertion times out). Decision: a =_closed= flag
raising after close, or reword the comments to what the locks do
guarantee.

** UI

*** TODO [#C] Dispatch form's Start-at picker uses the browser TZ  :ui:
=dispatch-form.js= interprets the =datetime-local= value in the
browser's zone while every displayed timestamp (including the
resulting dispatch row) renders in the sim TZ from the TZ chip — a
UTC-browser operator scheduling "14:00" sees the table show 16:00.
Decision: interpret the picker in =clockState.tzInUse()= or label
the field with the browser zone.

** Tests / CI / packaging

*** TODO [#C] scenario.rs energy-integral tolerance can flake :testing:
=driver_run_aggregates…= asserts 8..=12 Wh around an analytic 10 Wh
from manual timestamp-driven ticks, but the fixture's background
physics loop concurrently advances the same accumulator at wall
clock (~1 Wh/s at the 3.6 kW setpoint) — a ~2 s scheduler stall
pushes past 12. Decision: widen the tolerance, or add a fixture flag
that skips background physics for manually-ticked tests.

*** TODO [#C] wheels.yml path filter omits python/src/**      :packaging:ci:
The pull_request filter covers packaging manifests but not the pure-
Python package the wheel bundles, so a PR breaking =runtime.py='s
import passes python.yml (lint/type only) and surfaces at the next
tag build. The filter's comment documents the cost tradeoff for the
Rust globs only. Decision: add =python/src/**= (each entry is a full
wheel-matrix build) or note the exclusion as deliberate.

* Open design questions

** d1. Rethink ~file-exists-p~ resolving against the config's load dir :design:lisp:

The current =defuns::fs::register= (=src/lisp/defuns/fs.rs=) resolves
relative paths in =(file-exists-p PATH)= against the config file's
load directory (the same base =(load PATH)= uses). The motivation
was to keep =load-overrides='s gate

#+begin_src elisp
(when (file-exists-p "microgrids/config.7.overrides.lisp")
  (load "microgrids/config.7.overrides.lisp"))
#+end_src

answering the same question both halves of that =when= ask — the
two used to disagree when the process CWD differed from the config
dir, which broke the override-replay path inside
=remove_persisted_override= and the test that exercises it.

That's still a real coordination problem, but the fix is a little
heavy-handed:

- *Emacs divergence.* Real Elisp's =file-exists-p= resolves against
  =default-directory=, not the load path. Anyone porting code from
  Emacs (or eyeballing the docstring) would expect CWD-relative
  resolution; ours quietly rebases.
- *Surprises for non-override paths.* =(file-exists-p
  "telemetry.csv")= etc. now also rebases — fine if the user
  always means "next to config.lisp" but wrong if they meant CWD.
- *Coupling to =load_dir= at registration time.* The closure
  captures the path on =Config::new=, so a later =set_load_path=
  on the ctx (if we ever expose one to Lisp) wouldn't be visible
  to =file-exists-p=. =(load PATH)= in tulisp reads =ctx.load_path=
  on every call.

*Better directions to consider:*

1. Drop the special handling in =file-exists-p= and instead change
   the Lisp side: =load-overrides= in =sim/common.lisp= could build
   an absolute path before the gate, eg. by exposing the
   override-file path as a Rust-side defun (=(overrides-path)=
   already exists; make it return an absolute path).
2. Land a tulisp-level fix: add a =ctx.load_path= getter (currently
   =pub(crate)=) so a closure can resolve at call time, mirroring
   =(load PATH)=.
3. Push a =file-exists-p= change upstream into tulisp itself,
   matching =(load)='s resolution.

(2) or (3) is probably the right answer; (1) is the smallest patch
on the switchyard side.

*Where the workaround lives today:* =defuns::fs::register= closes
over =load_dir: PathBuf=, propagated from =Config::new= via
=defuns::register_runtime=. The matching test
(=ui::tests::persisted_remove_drops_form_immediately=) inlines a
local =load-overrides= defun and relies on this resolution to find
the override file.

** d2. Promote per-mg override file into the per-mg config file :design:lisp:ui:

Currently every successful eval lands in the per-microgrid override
file (=microgrids/config.<id>.overrides.lisp=). The next step in
that flow: a UI action that takes the override file's contents and
folds them into the matching =microgrids/config.<id>.lisp= (inside
the =:topology= lambda — see below), then empties the override
file. After the action the world is unchanged but the source-of-
truth has shifted from "runtime edit" to "canonical microgrid
config" and the entry is no longer editable from the UI overrides
dialog.

*Where it'd land:* a button in the chrome (probably called Persist
or Promote), a server-side endpoint that does the file munging, and
an entry in this UI dialog that confirms the move.

*Open questions to settle before coding:*

- Where in =microgrids/config.<id>.lisp= does the content go? The
  natural slot is inside the =(make-microgrid :topology (lambda
  () …))= lambda body, alongside whatever topology the file
  already declares. Insertion point: before the trailing
  =(load-overrides)= call, so the canonical forms run first and
  then any remaining overrides layer on top. Mechanical append at
  the end of the lambda is the floor — semantic grouping (collect
  rename-component calls near the renamed components, etc.) is
  much harder and probably not worth automating in v1.
- Does the action try to dedup against existing forms in the
  per-mg config file? eg. if the override is
  =(rename-component 1 "alpha")= and the config already calls
  =(rename-component 1 "main-grid")= inside the topology, do we
  replace the existing call, append after it, or just append at
  the end and let last-write-win semantics carry the day?
- Does the action run =tulisp-fmt= over the merged file, or
  preserve the existing whitespace / comments verbatim and only
  format the appended block?
- After the move, the override file is empty and the per-mg config
  file grew. Reloading should yield the same microgrid (same
  forms, just relocated). Worth a test that round-trips
  add → promote → reload → state-equal.

*Why deferred:* the immediate-write override file makes the
override file the pending log. Promote-to-config is a one-way
commit gate on top of that, but the design questions above need a
real config sample to test against — easier to settle once a heavy
microgrid config has accumulated some real overrides.

** d3. Parallel-paths share semantic — does this match real installs? :design:fidelity:

We currently model "child shared by N parents" as "each parent sees
1/N of the child's flow" (parallel-conductor semantic). It's
physically real for high-current buses split into N cables for
ampacity, each with its own CT and meter, summed by an upstream
aggregator. But that's an unusual SCADA-level topology.

The more common multi-parent setup in real microgrids is *redundant
observers* — two meters on the *same* wire (Class A + Class B,
primary + backup) both reporting the full flow, with the SCADA layer
deduping or selecting one. Under our current semantic, those two
meters would each read half, which isn't what the user expects.

If user feedback pushes us either way, options are:

- *Keep parallel-share as default*, document the redundant-observer
  use case as "express it via =:hidden t= on the backup meter so it
  doesn't appear in the connections graph".
- *Flip default to count-fully-per-parent*, and add an explicit
  =:share <fraction>= plist arg for the parallel-conductor case.
- *Add a topology-level kind annotation* (=:redundant-with <id>=,
  =:parallel-with <id>=) so the connection graph itself encodes
  intent. Most expressive, most invasive.

** d4. Per-phase reactive setpoints                         :design:reactive:

The proto's =SetElectricalComponentPower= takes a single scalar
=power= value, not per-phase. Real inverters can be commanded to
dispatch unbalanced reactive across phases for voltage support. Out
of scope for v1; would need a proto extension or a side-channel
defun. Tracked here so we don't forget the gap.

** d5. Per-source attribution under MxN                     :design:fidelity:

Today an inverter publishes its *commanded* AC output as =measured=;
the battery separately publishes the accepted (clamped) value. A
SCADA client that wants to see "inverter 1 produced 7.5 kW of the
13 kW total flowing into the shared battery" has to read both
streams and reconcile. A future upgrade: track per-source pushes on
the battery's accumulator (=Vec<(parent_id, p, q)>=), distribute
the clamp ratio across sources, and let each inverter read its own
=accepted_from(self.id)= to compute fair-share measured. Minor
trade-off in trait surface.

** d6. Persist-gate follow-ups (origin gating, form splitting) :design:lisp:

The core landed 2026-06-12: =Config::eval= persists a source to the
per-mg overrides file only when it moved the enterprise structural
fingerprint (register / connect / disconnect / remove / rename, plus
registry inserts) or contains a top-level =(setq *-defaults …)= —
so transient pokes no longer resurrect as config on reload. Settled
policy: defaults edits persist, renames persist, persistence is
origin-blind (REPL == canvas).

Deferred refinements, pick up if the need shows:
- *Origin gating (old option D)*: make the REPL scratch-by-default so
  structural experiments don't litter the overrides file. Needs an
  origin flag on the eval endpoints AND a visible "scratch — won't
  survive reload" indicator in the REPL, or it's a silent trap.
- *Form splitting (old option F)*: a mixed eval (one structural form +
  one poke) persists whole today; splitting top-level forms and
  persisting only the ones that moved the fingerprint closes that.
- *Net-zero edits*: an add+remove in one eval bumps the counter twice
  and persists despite changing nothing; a structure hash (old option
  A) layered on the counter would suppress it.

* Potential designs to consider — UX ideas (2026-08-20)
** TODO Live topology: values and flow on the canvas              :ux:design:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Nodes show current kW / SoC / health as badges; edges get flow
direction + thickness from live power. Makes Topology the screen
operators keep open instead of cross-referencing the dashboard.
(Large. Top-3 pick; design started on its own branch.)

** TODO Explain-this-number: formula tree with live values        :ux:design:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Click any dashboard value to see its formula tree with live values
substituted at every node: grid = #2 (-5.81 kW) = #3 (-24.0) + ...
The explain API already returns the tree; join it with live
telemetry. Turns "why is grid -5.8 kW" into one click. (Medium.
Top-3 pick.)

** TODO Command palette (Ctrl+K)                                  :ux:design:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Jump to any component by name/id (select + reveal), switch
microgrid/subview, run a scenario, eval a snippet. (Medium. Top-3
pick.)

** TODO Dashboard power-flow card                                 :ux:design:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Sankey-ish tile: grid <-> site, PV -> load/battery, battery <-> load
with live numbers on the arrows — "where is the power going" at a
glance instead of assembling it from seven tiles. (Medium-large.)

** TODO Dashboard alert strip                                            :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Components in error/standby as chips at the top of the dashboard
with jump-to-node links; today the signal hides in pulse-bar counts.
(Small.)

** TODO Scenario run timeline                                     :ux:design:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Cues, checks, events as markers on a horizontal time axis with a
progress cursor; pass/fail lands where it fired; optionally overlay
the key metric (battery power during an outage). (Medium.)

** TODO Dispatch calendar/timeline view                                  :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Draw the next 7 days of dispatch windows on a strip — recurrence as
text ("daily x2") hides overlaps and gaps. (Medium.)

** TODO Time scrubbing across live views                          :ux:design:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Shared pause + scrub control freezing every live view and walking
back through the last 15 min — the history rings already hold the
data. Replay instead of memory. (Large, most radical.)

** TODO Topology quick actions and edit lock                             :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Right-click: set power / health / mode without opening the
inspector (small-medium). Plus a view/edit lock toggle so accidental
drags can't rearrange a layout (small).

** TODO Landing cards with live mini-stats                               :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Grid-power sparkline + health dots per microgrid card instead of
static component counts. (Small.)

** TODO Inspector setpoint ergonomics                                    :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Slider bounded by the live envelope with a TTL picker instead of
bare number fields; show the command's remaining TTL ticking down.
(Small-medium.)

** TODO Inspector: pin and compare two components                        :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Pin a component's charts, open a second one side by side — e.g.
battery vs its inverter during a clamp. (Medium.)

** TODO Data-freshness indicator                                         :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

A live/stale badge driven by WS heartbeat: when the socket drops or
the tab was throttled, views say "stale since hh:mm:ss" instead of
silently showing old numbers; reconnect toast. (Small-medium.)

** TODO Structured log pane                                              :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Filter by level/component, search, click a component id in a log
line to select its node. (Medium.)

** TODO REPL history browser and snippets                                :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Persisted eval history with search; a saved-snippets panel for
common commands (set-power loops, scenario starts). (Small-medium.)

** TODO Subtree collapse for large topologies                            :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Collapse a meter's subtree into one summary node (sum of powers,
worst health) for 100+-component sites; expand on click. (Medium-
large.)

** TODO Scenario run comparison                                          :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:12]
:END:

Diff two runs' reports side by side (checks passed/failed, peak
stats) — regression checking after a physics change. (Medium.)

** TODO Per-row mini sparklines                                          :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:13]
:END:

A tiny power/SoC history inline in each component row — trend
without opening the inspector. (Small-medium; watch render cost
with many rows.)

** TODO Export surfaces                                                  :ux:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 15:13]
:END:

PNG export of the topology canvas; CSV download buttons where the
data already exists (history windows, scenario CSVs are served
already). (Small.)

** TODO Pill nodes: colour that pops at every zoom level             :ux:design:
:LOGBOOK:
- State "TODO"       from              [2026-08-20 Do 23:40]
:END:

The pill redesign (branch live-topology) moved category colour from
the whole ellipse to a 9 px dot, and the pill surface (#242a33) sits
only a few steps above the background (#1c2128). Zoomed out on a
large graph the nodes barely register and the dot is sub-pixel; the
zoomed-in look is right and should stay.

Zoom-independent changes first (small, reversible, spec-able now):
- Replace the dot with a 4 px category bar on the pill's left edge
  (clipped to the rounded corner). A bar reads as colour at small
  sizes where a dot vanishes, and frees ~13 px of width.
- Border contrast: #323a45 -> ~#3d4654 at 1.25 px. Biggest win for
  "are there nodes here" at any zoom, no colour change.
- Live tint: blend 6-8 % of the hero colour into the surface (green
  exporting, blue importing, neutral when dead). Subliminal at 1x,
  the only "alive" signal when zoomed out.
- Move the pill / hover-card palette (pill.js COLORS, .hover-card
  CSS) onto :root tokens like the rest of the UI so a re-theme
  reaches the canvas; today's literals deliberately differ from
  --bg-elev/--border/--fg and should become named tokens.

Semantic zoom second (the renderer redraws every frame and
topology.js has network.getScale(), so LOD per draw is cheap):
- >= 0.8: today's look (+ the bar).
- 0.4-0.8: surface one step lighter (#2a313b), border a ~35 % tint
  of the category colour, hero number only, name dropped.
- < 0.4: no text; the surface takes the category colour at ~55 %
  saturation (the old "pop"); hidden = dashed outline; selection
  ring unchanged.
Thresholds are guesses — mock a 60-node graph at three zooms (like
the node-studies artifact) before fixing them. Hysteresis around
each threshold so panning at the boundary doesn't flicker.
(Medium. Requested 2026-08-20 after the pill redesign.)
